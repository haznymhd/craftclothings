import React from "react";
import Recent from "./Recent"; // Ensure this path is correct based on your directory structure

const RecentPage = () => {
  return (
    <Recent showAll={true} />
  );
}

export default RecentPage;
